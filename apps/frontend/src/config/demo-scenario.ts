/**
 * 発表で見せる操作の並び（FE-21）。
 *
 * ここに書く操作は、いまの main の画面で実際にできることだけにする。
 * 画面に無いものを台本に書くと、発表の最中にその手順で止まる。
 * まだ画面に無い話（安否の共有範囲や履歴など）は台本から外し、
 * `avoid` に「今日は触れない」として明記する。
 *
 * 逆に、画面に入ったものを台本から外したままにするのも同じくらい悪い。
 * 避難の選択肢と切り替え基準（FE-14 / BE-19）は #50 で画面に入ったため、
 * 手順 4 を「徒歩の候補を読む」から「選択肢を作って切り替え基準を読む」へ直した。
 *
 * 台本を文章だけで持つと、画面が増減したときに古くなったことに誰も気づかない。
 * ステップを配列で持ち、行き先の画面が実在するかをテストで検査する。
 *
 * 手順 1〜3 を投稿タブ（/posts）で通すのは、投稿を取得して地図へ出すのが
 * いまはこの画面だけだからである。地図タブ（/）は fieldReport.list を呼ばず、
 * 固定のサンプル表示しか出さないため、投稿しても何も変わらない。
 * 地図タブが投稿を出すようになったら、手順 1 と 3 の route を戻す。
 *
 * 準備の手順は docs/demo/scenario.md にある。
 */

export type DemoStep = {
  /** 通し確認の記録に使う。並びを変えても記録が壊れないよう order とは別に持つ */
  id: string;
  order: number;
  title: string;
  /** 開く画面。src/app 配下に実在することをテストで検査する */
  route: string;
  /** 実際に指を動かす操作 */
  operation: string;
  /** そのとき口で言うこと */
  talkingPoint: string;
  /** この画面を見せるのに要るデータ。`pnpm demo:seed` で入るものを前提にする */
  requires: string;
  /** まだ画面に無いので、発表では触れないこと。触れて良いなら null */
  avoid: string | null;
  /** その場で詰まったときの逃げ道。逃げ道が要らない手順は null */
  fallback: string | null;
};

export const demoScenario: readonly DemoStep[] = [
  {
    id: "map-overview",
    order: 1,
    title: "近所の道の状況を地図で見る",
    route: "/posts",
    operation:
      "下のナビゲーションから「投稿」を開く。地図に出ている件数付きの吹き出しピンと、左下の凡例（通行可・注意・通行不可）を指し示す",
    talkingPoint:
      "災害時に最初に知りたいのは『いま、その道が通れるか』です。予報でも避難所の場所でもなく、目の前の道です。",
    requires:
      "`pnpm demo:seed` で入る真備町の投稿12件と、demo-sato@michinavi.example でのログイン",
    avoid:
      "地図タブ（/）は投稿を取りに行かないサンプル表示のままなので、デモでは開かない",
    fallback:
      "ピンが出ないときは、ヘッダーの「投稿◯件」を見る。0件なら位置情報が真備町から外れて絞り込みが変わっている。再読み込みして初期表示の真備町へ戻す",
  },
  {
    id: "post-report",
    order: 2,
    title: "その場の状況を3タップで投稿する",
    route: "/posts",
    operation:
      "地図右上の「現在地を追跡」で投稿地点を確定し、下の「この道の状況を報告する」を開く。そこから「通れない」→「冠水」→「投稿する」の3タップで送る",
    talkingPoint:
      "一次情報は住民しか持っていません。文章を書かせず、3タップで送れることが投稿数を決めます。位置は約250mのメッシュに丸めてから送るので、家の座標そのものは残りません。",
    requires:
      "ログイン済みであること。ブラウザの位置情報を許可し、デモ位置（34.6383, 133.6903）に固定しておくこと",
    avoid:
      "原因（冠水など）の選択はまだ保存されない。画面にもその断りが出るので、保存されるのは通行状態と地点だけだと自分から言う",
    fallback:
      "「投稿にはログインが必要です」と出たら /login で demo-sato@michinavi.example / demo-password-1234 で入り直す",
  },
  {
    id: "map-after-post",
    order: 3,
    title: "投稿が同じ地点のピンにまとまる",
    route: "/posts",
    operation:
      "投稿後に戻ってきた地図で「投稿しました」を確認し、いま投稿した地点のピンを開く。件数が1つ増え、色が通行不可に変わっている",
    talkingPoint:
      "同じ場所への投稿は1つの吹き出しにまとまります。件数が多い地点ほど、その状態の確からしさが上がります。",
    requires:
      "手順2の投稿が保存されていること。投稿後に一覧を取り直すので、画面の再読み込みは要らない",
    avoid:
      "ピンの色は投稿そのもの（同じ地点の最新の状態）から決めている。AI の推定は保存しているが画面には出ないので、推定の話はしない。吹き出しの各投稿に並ぶ「この投稿を通報する」（FE-18）は動くが、今日は押さない",
    fallback:
      "件数が変わらないときは、ヘッダーの「投稿◯件」が1件増えたことを見せて先へ進む",
  },
  {
    id: "evacuation-options",
    order: 4,
    title: "避難の選択肢と切り替え基準を見る",
    route: "/evacuation",
    operation:
      "「避難経路」タブを開き、地図の下の「デモ位置」を押して避難先を割り当てる。下半分に「避難の選択肢はまだありません」が出るので「選択肢を作る」を押す。並んだカード（1件目に「推奨」）から「徒歩で避難する」を開き、理由・注意点・「切り替えを考える目安」を読み上げる。最後に「避難所」タブへ移り「デモ位置」→一覧から1件開いて受け入れ条件を見せる",
    talkingPoint:
      "アプリは『避難してください』とは言いません。世帯の人数と車の有無、周辺の投稿から、その世帯が取りうる選択肢だけを並べ、それぞれの弱点と『どうなったら切り替えるか』を出します。決めるのは本人です。",
    requires:
      "`pnpm demo:seed` で入るデモ佐藤家（大人2人・子1人・車1台）と、`pnpm db:reset` で入る seed.sql の避難所（デモ第一小学校など、すべて架空の施設）。選択肢はログイン中の世帯から作るので demo-sato@michinavi.example のままでいること。GEMINI_API_KEY が無くてもサーバ側の定型文で選択肢は出る",
    avoid:
      "選択肢の件数と並びは自宅周辺の道路状況の推定で変わる（「通れない」の推定があると「建物の上階へ移る」が増える）ので、「必ず3件出ます」とは言わない。徒歩の選択肢に出る分数はサーバがまだ所要時間を持っていないための直線距離の目安で、画面にもその断りが出る",
    fallback:
      "「選択肢を作成できませんでした」なら、そのとき出ているボタン（一覧がまだ無ければ「選択肢を作る」、一覧が出ていれば見出し横の「更新」）をもう一度押す。「世帯情報が必要です」ならログインしている世帯が違う。それでも出ないときは避難所タブの受け入れ条件だけで話を続ける。避難所が「周辺に避難所が見つかりませんでした」なら避難所タブの「デモ位置」を押して真備町箭田へ戻す",
  },
  {
    id: "family-status",
    order: 5,
    title: "家族の状況を共有する",
    route: "/family",
    operation:
      "「家族」タブを開き、デモ佐藤家の3人（自分・配偶者・長男）が「未確認」で並んでいるのを見せる。自分の状態ボタンから「避難済み」を選び、続けて配偶者の欄で「支援が必要と伝える」を押して赤い「支援が必要です」を出す",
    talkingPoint:
      "地図は出しません。居場所ではなく『無事かどうか』だけを共有します。アカウントを持たない家族のぶんは、同居している人が代わりに登録できます。",
    requires:
      "`pnpm demo:seed` で入るデモ佐藤家（3人）と、demo-sato@michinavi.example でのログイン。安否はシードに含めないので、開いた直後は全員「未確認」で正しい",
    avoid:
      "誰に見せるか（共有範囲）の設定と安否の履歴はAPIにはあるが画面に無いので、その話はしない。支援の要否は状態とは別のボタンで、状態の選択肢には出てこない",
    fallback:
      "「世帯がまだ登録されていません」と出たら `pnpm demo:seed` が済んでいない。「家族の安否を見るにはログインが必要です」なら /login で入り直す",
  },
] as const;

/** 発表の持ち時間（分）。1 周がこの中に収まるかを通し確認で計る */
export const demoTimeboxMinutes = 5;
